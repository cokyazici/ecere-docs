uint64 aUint64; // A stand alone declaration of a uint64.<br>
uint64 a, b; // Declaring two uint64s at the same time.<br>
uint64 c = 64000, d = 10; // Declaring two uint64s, and initializing them both at the same time.